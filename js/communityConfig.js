// communityConfig.js — one place to point the Community tab at its backend.
// See server/README.md for how it's deployed. If this is ever blank, the
// Community tab disables itself gracefully with an explanation instead of
// failing every request against a nonexistent API.
export const COMMUNITY_API_BASE = 'https://autocircuit-community-api.onrender.com';
